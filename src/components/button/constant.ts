import { LucideIcon } from "lucide-react"

export type plainButtonType = {
    fill: string,
    background: string,
    icon: LucideIcon
}

export type textButtonType = {
    text: string,
    icon: LucideIcon
}